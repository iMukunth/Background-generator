# Gradient Background Generator
This project contain solution for task in ZTM.

This JavaScript code provides functionality to create a gradient background for an HTML element using two color inputs. It also provides a button to generate random colors for those inputs.

In summary, this HTML file provides a simple user interface for creating and customizing gradient backgrounds with the help of JavaScript code.
